import Day from '@/assets/images/Day.gif';
import styles from '@/styles/commons/LoginFrame.module.css';

const LoginFrame = () => {
  return <img className={styles.LoginFrame} src={Day} alt="DayTemplate" />;
};

export default LoginFrame;
